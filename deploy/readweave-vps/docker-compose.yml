services:
  readweave:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${READWEAVE_IMAGE:-readweave:0.104.1}
    container_name: readweave
    restart: unless-stopped
    env_file:
      - __READWEAVE_VERIFIER_ENV_FILE__
    ports:
      - "${READWEAVE_BIND:-127.0.0.1:11802}:8080"
    volumes:
      - ${READWEAVE_DATA_DIR:-__READWEAVE_DATA_DIR__}:/home/node/trilium-data
